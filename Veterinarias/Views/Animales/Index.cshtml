@model IEnumerable<Veterinarias.Modelos.Animales>

<div class="mail-box">
    <h2>Animales</h2>
    <p>
        <a asp-action="Create" class="btn btn-primary add-dialog-window">Nuevo Registro</a>
    </p>
    <div class="table-responsive table-sm table-hover">
        <table id="example" class="table table-hover table-mail">
            <thead class="table-active">
                @*crear la fila con los criterios:*@
                <tr>
                    <th>
                        Animal
                    </th>
                    <th>
                        Estado
                    </th>
                    <th>
                        Acciones
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Nombre)
                        </td>
                        <td>
                            @* @Html.DisplayFor(modelItem => item.Estado) ESTO HARA QUE HAYA UN CHECK LIST*@ 
                            <span class="estado-badge @(item.Estado ? "badge-success" : "badge-danger")">
                                @(item.Estado ? "Activado" : "Anulado")
                            </span>
                        </td>
                        <td>
                            <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-warning edit-dialog-window">Editar</a>
                            @if (item.Estado)
                            {
                                <a data-id="@item.Id" class="btn btn-danger anular-dialog-window">Anular</a>
                            }
                            else
                            {
                                <a data-id="@item.Id" class="btn btn-success activar-dialog-window">Activar</a>
                            }
                            <a asp-controller="Razas" asp-action="Index" asp-route-id="@item.Id" class="btn btn-info">Razas</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

<div class="modal fade" id="Activar" tabindex="-1" role="dialog" aria-labelledby="modalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header" style="background-color:#3fb618;color:white;">
                <button data-dismiss="modal" class="close" type="button">
                    <span aria-hidden="true">×</span>
                    <span class="sr-only">Close</span>
                </button>
                <h2 class="modal-title">Activar</h2>
            </div>
            <div class="modal-body">
                <div class="text-center">
                    <h4>¿Estás seguro de activar el animal seleccionado?</h4>
                </div>
                <div id="responseActivar" class="text-right">
                </div>
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="Anular" tabindex="-1" role="dialog" aria-labelledby="modalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header" style="background-color:#da2213;color:white;">
                <button data-dismiss="modal" class="close" type="button">
                    <span aria-hidden="true">×</span>
                    <span class="sr-only">Close</span>
                </button>
                <h2 class="modal-title">Anular</h2>
            </div>
            <div class="modal-body">
                <div class="text-center">
                    <h4>¿Estás seguro de anular el animal seleccionado?</h4>
                </div>
                <div id="responseInactivar" class="text-right">
                </div>
            </div>
        </div>
    </div>
</div>

<div class="modal fade bd-example-modal-lg" tabindex="-1" role="dialog" aria-labelledby="myLargeModalLabel" id="AddModal" aria-hidden="true">
    <div class="modal-dialog modal-lg" style="width:50%;">
        <div class="modal-content">
            <div class="modal-header" style="background-color:green;color:white;">
                <h5 class="modal-title" id="TittleCreatemodel">Nuevo Animal</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <div class="teeee">Please wait...</div>
            </div>
        </div>
    </div>
</div>

<div class="modal fade bd-example-modal-lg" tabindex="-1" role="dialog" aria-labelledby="myLargeModalLabel" id="EditModal" aria-hidden="true">
    <div class="modal-dialog modal-lg" style="width:50%;">
        <div class="modal-content">
            <div class="modal-header" style="background-color:orange;color:white;">
                <h5 class="modal-title" id="TittleCreatemodel">Editar Animal</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <div class="teeee">Please wait...</div>
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="Anular" tabindex="-1" role="dialog" aria-labelledby="modalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header" style="background-color:#da2213;color:white;">
                <button data-dismiss="modal" class="close" type="button">
                    <span aria-hidden="true">×</span>
                    <span class="sr-only">Close</span>
                </button>
                <h2 class="modal-title">Eliminar animal</h2>
            </div>
            <div class="modal-body">
                <div class="text-center">
                    <h4>¿Estas seguro de eliminar el registro seleccionado?</h4>
                </div>
                <div id="response" class="text-right">
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    
    <script>
        //PARA ANULAR
        $("body").on("click", "a.anular-dialog-window", null, function (e) {
            var htmlContent = '<br /><button type="button" id="btnNo" class="btn btn-danger" style="background-color:#da2213;color:white;" data-dismiss="modal" aria-hidden="true">No</button>&nbsp&nbsp<a href="@Url.Action("Anular", "Animales")/' + $(this).data('id') + '" class="btn btn-default">Sí</a>';
            $("#responseInactivar").html(htmlContent);
            $('#Anular').modal('show');
        });
        //PARA ACTIVAR
        $("body").on("click", "a.activar-dialog-window", null, function (e) {
            var htmlContent = '<br /><button type="button" id="btnNoActivar" class="btn btn-danger" style="background-color:#da2213;color:white;" data-dismiss="modal" aria-hidden="true">No</button>&nbsp&nbsp<a href="@Url.Action("Activar", "Animales")/' + $(this).data('id') + '" class="btn btn-success">Sí</a>';
            $("#responseActivar").html(htmlContent);
            $('#Activar').modal('show');
        });
    </script>
    
    <script>
        //PARA ABRIR UNA VENTANA CREATE
        //PARA QUE NO SEA UNA PESTAÑA LE PONEMOS 'VIEW' EN CONTROLLER Y ESTE SCRIPT SE VA PARA EL MISMO CREATE
        $("body").on("click", "a.add-dialog-window", null, function (e) {
            e.preventDefault();
            var $link = $(this);
            var url = $(this).attr('href');
            if (url.indexOf('#') == 0) {
                $('#AddModal').modal('show');
            }
            else {
                $.get(url, function (data) {
                    $('#AddModal .teeee').html(data);
                    $('#AddModal').modal('show');
                });
            }
        });
    </script>

    <script>
        //PARA ABRIR UNA VENTANA EDIT
        //PARA QUE NO SEA UNA PESTAÑA LE PONEMOS 'VIEW' EN CONTROLLER Y ESTE SCRIPT SE VA PARA EL MISMO EDIT
        $("body").on("click", "a.edit-dialog-window", null, function (e) {
            e.preventDefault();
            //debugger;
            var $link = $(this);
            var url = $(this).attr('href');
            if (url.indexOf('#') == 0) {
                $('#EditModal').modal('show');
            }
            else {
                $.get(url, function (data) {
                    $('#EditModal .teeee').html(data);
                    $('#EditModal').modal('show');
                });
            }
        });
    </script>

    @*<script>
        //PARA ABRIR UNA VENTANA DELETE
        //PARA QUE NO SEA UNA PESTAÑA LE PONEMOS 'VIEW' EN CONTROLLER Y ESTE SCRIPT SE VA PARA EL MISMO DELETE
        $("body").on("click", "a.delete-dialog-window", null, function (e) {
            var htmlContent = '<br /><button type="button" id="btnNo" class="btn btn-danger" style="background-color:#da2213;color:white;" data-dismiss="modal" aria-hidden="true">No</button>&nbsp&nbsp<a href="@Url.Action("Delete", "Productos")/' + $(this).data('id') + '" class="btn btn-default">Si</a>';
            $("#response").html(htmlContent);
            $('#Anular').modal('show');
        });
        //Cuadro para poder eliminar registro, con boton no y si
    </script>*@


    @*PARA BUSCAR ALGUN DATO Y OBTENERLO
        //SI ESTA DENTRO DE UNA PESTAÑA CREATE O EDIT SE PONE ESTE SCRIPT EN SU MISMO ARCHIVO
        <script>
        function onclickBuscarProveedor() {
            debugger;
            var parametro = $('#parametroBusqueda').val();
            var url = '@Url.Action("BuscarProveedor", "Productos")';
            url += '?busqueda=' + parametro;
            $.ajax({
                type: 'GET',
                url: url,
                dataType: 'html',
                success: function (data) {
                    $('#ProveedorModal .teeee').html(data);
                    $('#ProveedorModal').modal('show');
                },
                error: function (ex) {
                    alert('Failed to retrieve cities.' + ex);
                }
            });
        }
    </script>

    <script>
        function GuardarE3() {
            debugger
            $('#ProveedorModal').modal('hide');
            if (dato1 != null && dato2 != null) {
                $('.nom_prove').val(dato3);
                document.getElementById('IdProveedores').value = dato1;
                $('#ruc_prove').val(dato2);
            }
        }</script>

    <script>
        $(document).on('click', '#tablaBuscarProveedor3 tbody tr', function () {
            debugger;
            //Selecciona fila.
            let TotalColumnas = $("#tablaBuscarProveedor3").find("tr");
            $(TotalColumnas).each(function () {
                $(this).removeClass('active');
            });
            $(this).addClass('active');
            //Obtiene valores de selección
            dato1 = $(this).find("td").eq(0).text().trim();
            dato2 = $(this).find("td").eq(1).text().trim();
            dato3 = $(this).find("td").eq(2).text().trim();
        });
    </script>*@

   @*<script>
        //PARA HACER EL COMBO ANIDADO
        //SI ESTA DENTRO DE UNA PESTAÑA CREATE O EDIT SE PONE ESTE SCRIPT EN SU MISMO ARCHIVO
        function onchangeZonas() {
            let idZona = $("#comboZona").val();
            CargarCategorias(idZona);
        }
        function CargarCategorias(idZona) {
            fetchGet("/Productos/CargarCategorias/?id=" + idZona, function (data) {
                llenarCombo(data, "comboCategoria", "nombreCategoria", "id", "0");
            });
        }
    </script>*@


    @*Para ordenar la tabla

    <script>
        $(document).ready(function () {
            //ESTE ES PARA EL DATATABLES
            $('#example').DataTable({
                language: {
                    search: "Buscar: ",
                    info: "Mostrando _START_ a _END_ de _TOTAL_ elementos",
                    infoFiltered: "(filtrado de _MAX_ registros en total)",
                    paginate: {
                        first: "Primero",
                        previous: "<",
                        next: ">",
                        last: "Último"
                    }
                },
                "order": [[0, "asc"]],
                stateSave: true,
                dom: 'Bfrtip',
                buttons: [
                    'excelHtml5',
                    'pdfHtml5'
                ]
            });
        });
    </script>*@

    }

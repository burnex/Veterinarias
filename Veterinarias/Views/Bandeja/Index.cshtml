@model IEnumerable<Veterinarias.Modelos.PR_MASCOTAS_S01>

@*<div class="mail-box">
    <h2>Veterinarios</h2>
    <p>
        <a asp-action="Create" class="btn btn-primary add-dialog-window">Nuevo Registro</a>
    </p>*@
<div class="mail-box">
    <h2>Mascotas</h2>
    <div class="form-row mb-3">
        <div class="col">
            <label for="animal">Animal:</label>
            <select id="animal" class="form-control">
                <option value="">Todos</option>
                <option value="DNI">DNI</option>
                <option value="Carnet de Extranjería">Carnet de Extranjería</option>
                <option value="Pasaporte">Pasaporte</option>
            </select>
        </div>
        <div class="col">
            <label for="raza">Raza:</label>
            <select id="raza" class="form-control">
                <option value="">Ambos</option>
                <option value="M">Masculino</option>
                <option value="F">Femenino</option>
            </select>
        </div>
        <div class="col">
            <label for="dueño">Dueño:</label>
            <select id="dueño" class="form-control">
                <option value="">Ambos</option>
                <option value="bruno">Bruno</option>
                <option value="mercedes">Mercedes</option>
                <option value="mafer">Mafer</option>
                <option value="vale">Vale</option>
            </select>
        </div>
        <div class="col align-self-end">
            <button type="button" class="btn btn-primary">Aplicar Filtros</button>
            <button type="button" class="btn btn-primary">Limpiar filtros</button>
        </div>
    </div>
    <p>
        <a asp-action="Create" class="btn btn-primary add-dialog-window">Nuevo Registro</a>
    </p>
    <div class="table-responsive table-sm table-hover">
        <table id="example" class="table table-hover table-mail">
            <thead class="table-active">
                <tr>
                    <th>
                        Animal
                    </th>
                    <th>
                        Raza
                    </th>
                    <th>
                        Nombres
                    </th>
                    <th>
                        Fecha nac
                    </th>
                    <th>
                        Edad
                    </th>
                    <th>
                        Color
                    </th>
                    <th>
                        Foto
                    </th>
                    <th>
                        Dueño
                    </th>
                    <th>
                        Estado
                    </th>
                    <th>
                        Acciones
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Animal)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Raza)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Nombres)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.FechaNacimiento)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Edad)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Color)
                        </td>

                        <td>
                            @if (!string.IsNullOrEmpty(item.Foto))
                            {
                                <img src="@item.Foto" class="img-listado" alt="Foto de la mascota" />
                            }
                        </td>

                        <td>
                            @Html.DisplayFor(modelItem => item.Dueño)
                        </td>
                        <td>
                            <span class="estado-badge @(item.Estado == "Registrado" ? "badge-secondary" : (item.Estado == "Aprobado" ? "badge-success" : "badge-danger"))">
                                @(item.Estado == "Registrado" ? "Registrado" : (item.Estado == "Aprobado" ? "Aprobado" : "Rechazado"))
                            </span>
                        </td>

                        <td>
                            @if (item.Estado == "Registrado")
                            {
                                <a data-id="@item.Id" class="btn btn-success aprobar-dialog-window">Aprobar</a>
                                <a data-id="@item.Id" class="btn btn-danger rechazar-dialog-window">Rechazar</a>
                            }

                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

<div class="modal fade" id="Aprobar" tabindex="-1" role="dialog" aria-labelledby="modalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header" style="background-color:#3fb618;color:white;">
                <button data-dismiss="modal" class="close" type="button">
                    <span aria-hidden="true">×</span>
                    <span class="sr-only">Close</span>
                </button>
                <h2 class="modal-title">Activar</h2>
            </div>
            <div class="modal-body">
                <div class="text-center">
                    <h4>¿Estás seguro de aprobar a la mascota seleccionado?</h4>
                </div>
                <div id="responseAprobar" class="text-right">
                </div>
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="Rechazar" tabindex="-1" role="dialog" aria-labelledby="modalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header" style="background-color:#da2213;color:white;">
                <button data-dismiss="modal" class="close" type="button">
                    <span aria-hidden="true">×</span>
                    <span class="sr-only">Close</span>
                </button>
                <h2 class="modal-title">Anular</h2>
            </div>
            <div class="modal-body">
                <div class="text-center">
                    <h4>¿Estás seguro de rechazar a la mascota seleccionado?</h4>
                </div>
                <div id="responseRechazar" class="text-right">
                </div>
            </div>
        </div>
    </div>
</div>

<div class="modal fade bd-example-modal-lg" tabindex="-1" role="dialog" aria-labelledby="myLargeModalLabel" id="AddModal" aria-hidden="true">
    <div class="modal-dialog modal-lg" style="width:50%;">
        <div class="modal-content">
            <div class="modal-header" style="background-color:green;color:white;">
                <h5 class="modal-title" id="TittleCreatemodel">Crear Registro</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <div class="teeee">Please wait...</div>
            </div>
        </div>
    </div>
</div>


@section Scripts {

    <script>
        $("body").on("click", "a.rechazar-dialog-window", null, function (e) {
            var htmlContent = '<br /><button type="button" id="btnNo" class="btn btn-danger" style="background-color:#da2213;color:white;" data-dismiss="modal" aria-hidden="true">No</button>&nbsp&nbsp<a href="@Url.Action("Anular", "Bandeja")/' + $(this).data('id') + '" class="btn btn-default">Sí</a>';
            $("#responseRechazar").html(htmlContent);
            $('#Rechazar').modal('show');
        });

        $("body").on("click", "a.aprobar-dialog-window", null, function (e) {
            var htmlContent = '<br /><button type="button" id="btnNoActivar" class="btn btn-danger" style="background-color:#da2213;color:white;" data-dismiss="modal" aria-hidden="true">No</button>&nbsp&nbsp<a href="@Url.Action("Activar", "Bandeja")/' + $(this).data('id') + '" class="btn btn-success">Sí</a>';
            $("#responseAprobar").html(htmlContent);
            $('#Aprobar').modal('show');
        });
    </script>

    <script>
        //PARA ABRIR UNA VENTANA CREATE
        $("body").on("click", "a.add-dialog-window", null, function (e) {
            e.preventDefault();
            var $link = $(this);
            var url = $(this).attr('href');
            if (url.indexOf('#') == 0) {
                $('#AddModal').modal('show');
            }
            else {
                $.get(url, function (data) {
                    $('#AddModal .teeee').html(data);
                    $('#AddModal').modal('show');
                });
            }
        });
    </script>

    <script>
        $(document).ready(function () {
            //ESTE ES PARA EL DATATABLES
            $('#example').DataTable({
                language: {
                    search: "Buscar: ",
                    info: "Mostrando _START_ a _END_ de _TOTAL_ elementos",
                    infoFiltered: "(filtrado de _MAX_ registros en total)",
                    paginate: {
                        first: "Primero",
                        previous: "<",
                        next: ">",
                        last: "Último"
                    }
                },
                "order": [[0, "asc"]],
                stateSave: true,
                dom: 'Bfrtip',
                buttons: [
                    'excelHtml5',
                    'pdfHtml5'
                ]
            });
        });
    </script>
    }